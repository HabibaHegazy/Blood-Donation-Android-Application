<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp">

    <Spinner
        android:id="@+id/days_of_Week"
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:textAlignment="center"
        android:layout_margin="10dp"
        android:textColor="@color/black"
        android:textSize="14dp"
        android:background="@color/white_greyish"
        android:entries="@array/days_of_week"
        android:ems="15" />

    <EditText
        android:id="@+id/timingSlotEditText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/days_of_Week"
        android:layout_alignParentStart="true"
        android:layout_alignParentEnd="true"
        android:layout_marginStart="16dp"
        android:drawablePadding="10dp"
        android:hint="timing slot"
        android:text="1:00 PM - 1:30 PM"
        android:inputType="text">

        <requestFocus />
    </EditText>

    <Button
        android:id="@+id/add_edit_slot"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/bloodRed"
        android:layout_below="@+id/timingSlotEditText"
        android:text="Add Timing Slot"
        android:textSize="18sp"
        android:textColor="@color/white"
        android:layout_marginBottom="20dp"/>

</RelativeLayout>